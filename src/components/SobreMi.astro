<section id="sobre">
  <div class="col-left">
    <div class="section-header">
      <span class="section-num">01</span>
      <h2>Sobre mí</h2>
      <div class="section-line"></div>
    </div>
    <div class="col-text">
      <p>Soy <strong>desarrollador Full Stack</strong> con background en entornos industriales de alta exigencia. Me reconvertí al desarrollo web combinando la disciplina y atención al detalle del trabajo en manufactura con la capacidad de construir aplicaciones completas y escalables.</p>
      <p>Actualmente tengo un <strong>proyecto real en producción</strong> en una aplicación web de gestión integral para un estudio de pole dance en Girona, desarrollada como TFM del Máster en desarrollo Full Stack en Campus Atrium.</p>
      <p>Mi stack principal es <strong>Angular 19 + Spring Boot 3 + MySQL 8</strong>, con experiencia en despliegue en VPS, SEO técnico, autenticación JWT y cumplimiento RGPD.</p>
    </div>
  </div>

  <div class="col-right">
    <div class="stats-grid">
      <div class="stat">
        <div class="stat-num">5+</div>
        <div class="stat-label">Lenguajes aprendidos</div>
      </div>
      <div class="stat">
        <div class="stat-num">1+</div>
        <div class="stat-label">Años como desarrollador Full Stack</div>
      </div>
      <div class="stat">
        <div class="stat-num">1</div>
        <div class="stat-label">Proyectos en producción</div>
      </div>
      <div class="stat">
        <div class="stat-num">∞</div>
        <div class="stat-label">Ganas de aprender</div>
      </div>
    </div>
  </div>
</section>

<style>
  #sobre {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: start;
    padding: 7rem 3rem;
    border-top: 1px solid var(--line);
  }

  .section-header {
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  .section-num {
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.1em;
  }

  h2 {
    font-family: var(--serif);
    font-size: clamp(2rem, 4vw, 3.2rem);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .section-line {
    flex: 1;
    height: 1px;
    background: var(--line);
    margin-left: 1rem;
  }

  .col-text p {
    color: var(--muted);
    font-size: 13px;
    line-height: 1.9;
    margin-bottom: 1.2rem;
  }

  .col-text p strong {
    color: var(--ink);
    font-weight: 500;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1px;
    background: var(--line);
    border: 1px solid var(--line);
    margin-top: 5rem;
  }

  .stat {
    background: var(--bg);
    padding: 1.8rem;
  }

  .stat-num {
    font-family: var(--serif);
    font-size: 2.8rem;
    letter-spacing: -0.04em;
    line-height: 1;
    margin-bottom: 0.4rem;
    color: var(--ink);
  }

  .stat-label {
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  @media (max-width: 768px) {
    #sobre {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding: 4rem 1.5rem;
    }

    .stats-grid {
      margin-top: 0;
    }
  }
</style>
<section id="contacto">
  <div class="col-left">
    <div class="section-header">
      <span class="section-num">04</span>
      <h2>Contacto</h2>
      <div class="section-line"></div>
    </div>
    <p class="contact-intro">¿Tienes un proyecto <em>interesante</em> en mente?</p>
    <p class="contact-sub">
      Estoy disponible para proyectos freelance, colaboraciones y 
      posiciones full-time. No dudes en escribirme.
    </p>
    <div class="contact-links">
      <a href="mailto:ibarbosaferrer91@gmail.com" class="contact-link">
        <span class="lbl">Email</span>
        ibarbosaferrer91@gmail.com
      </a>
      <a href="https://linkedin.com/in/ignacio-barbosa-ferrer-5074b4130" target="_blank" class="contact-link">
        <span class="lbl">LinkedIn</span>
        ignacio-barbosa-ferrer
      </a>
      <a href="https://github.com/IBarbosaF" target="_blank" class="contact-link">
        <span class="lbl">GitHub</span>
        github.com/IBarbosaF
      </a>
      <a href="tel:+34635897547" class="contact-link">
        <span class="lbl">Teléfono</span>
        +34 635 897 547
      </a>
    </div>
  </div>

  <div class="col-right">
    <form class="contact-form" name="contact" method="POST">
      <div class="form-field">
        <label for="nombre">Nombre</label>
        <input type="text" id="nombre" name="nombre" placeholder="Tu nombre" />
      </div>
      <div class="form-field">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="tu@email.com" />
      </div>
      <div class="form-field">
        <label for="mensaje">Mensaje</label>
        <textarea id="mensaje" name="mensaje" placeholder="Cuéntame sobre tu proyecto..."></textarea>
      </div>
      <button type="submit" class="form-btn">Enviar mensaje →</button>
    </form>
  </div>
</section>

<style>
  #contacto {
    padding: 7rem 3rem;
    border-top: 1px solid var(--line);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .section-header {
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  .section-num {
    font-size: 11px;
    color: var(--accent);
    letter-spacing: 0.1em;
  }

  h2 {
    font-family: var(--serif);
    font-size: clamp(2rem, 4vw, 3.2rem);
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .section-line {
    flex: 1;
    height: 1px;
    background: var(--line);
    margin-left: 1rem;
  }

  .contact-intro {
    font-family: var(--serif);
    font-size: clamp(1.4rem, 2.5vw, 2rem);
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }

  .contact-intro em {
    font-style: italic;
    color: var(--accent);
  }

  .contact-sub {
    color: var(--muted);
    font-size: 13px;
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .contact-links {
    display: flex;
    flex-direction: column;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    color: var(--ink);
    text-decoration: none;
    font-size: 13px;
    padding: 1rem 0;
    border-bottom: 1px solid var(--line);
    transition: color 0.2s;
  }

  .contact-link:hover {
    color: var(--accent);
  }

  .lbl {
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
    min-width: 70px;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-field label {
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .form-field input,
  .form-field textarea {
    background: transparent;
    border: 1px solid var(--line);
    color: var(--ink);
    font-family: var(--mono);
    font-size: 13px;
    padding: 0.9rem 1rem;
    outline: none;
    transition: border-color 0.2s;
  }

  .form-field input:focus,
  .form-field textarea:focus {
    border-color: var(--ink);
  }

  .form-field textarea {
    resize: vertical;
    min-height: 140px;
  }

  .form-btn {
    background: var(--ink);
    color: var(--bg);
    border: none;
    font-family: var(--mono);
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 1rem 2rem;
    cursor: pointer;
    transition: background 0.2s, transform 0.2s;
    align-self: flex-start;
  }

  .form-btn:hover {
    background: var(--accent);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    #contacto {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding: 4rem 1.5rem;
    }
  }
</style>